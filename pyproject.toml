[project]
name = "generate-qr-code"
version = "0.1.0"
description = "Gerador de QR Codes estáticos"
requires-python = ">=3.10,<4.0"
dynamic = ["dependencies"]

[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
qrcode = {version = "^8.2", extras = ["pil"]}
